# 读取同目录下的 students.txt(每行格式:姓名 分数,UTF-8 编码)
#   1) 将分数 >= 60 的学生按原顺序写入 passed.txt(每行格式不变)
#   2) 将分数 < 60 的学生按原顺序写入 failed.txt(每行格式不变)
#   3) 分别打印两个文件各写入的人数
# 示例输出(用随包 students.txt 运行):
# passed.txt 写入 4 人
# failed.txt 写入 2 人
# 提示:with open(...) 读写文件;strip() 去掉行尾换行;split() 切分;int() 转换分数

# --------------在下面完成剩余代码,实现题目要求--------------------
