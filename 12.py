# 视觉题(大二必做,大一选做)
# 使用 OpenCV 完成(图片 12.png 与本文件在同一目录下):
#   1) 读取 12.png,转 HSV 后用 inRange 提取红色,再做形态学开运算去掉小噪点,
#      保存掩膜为 12_mask.png
#   2) 用 findContours 找出红色物体,过滤过小的(面积 < 500),统计数量并打印
#   3) 给每个物体画外接矩形并标注编号(1,2,3……),保存为 12_result.png

import cv2
import numpy as np

# --------------在下面完成剩余代码,实现题目要求--------------------
