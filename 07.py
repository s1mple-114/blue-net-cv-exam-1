# 题目变量定义(请勿修改)
# 每个元素为 (文件名, 宽, 高)
images = [
    ("a.png", 1920, 1080),
    ("b.png", 800, 600),
    ("c.png", 3840, 2160),
    ("d.png", 1024, 768),
    ("e.png", 2560, 1440),
]

# --------------在下面完成剩余代码,实现题目要求--------------------
# 1. 用列表推导式生成新列表 areas,元素为 (文件名, 面积)
# 2. 用 sorted(..., key=lambda ...) 按面积从大到小排序,打印前 3 名(每行格式:文件名 面积)
# 3. 用列表推导式筛选面积 >= 1000000 的文件名存入列表 big,并打印 big
# 输出示例:
# c.png 8294400
# e.png 3686400
# a.png 2073600
# ['a.png', 'c.png', 'e.png']
